Travel-And-Tourism/
├── index.html (main entry point)
├── assets/
│   ├── css/styles.css (global stylesheet)
│   └── js/main.js (cleaned JavaScript with modal, menu, viewport fix)
├── pages/
│   └── auth/
│       ├── login.html (standalone login page)
│       ├── signup.html (standalone signup page)
│       ├── auth.css (auth styles)
│       └── auth.js (auth scripts)
└── backup/ (original files for rollback)